# TECHXSPERTS Frontend

## Run the frontend

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the frontend:
   ```bash
   npm run dev
   ```

The frontend fetches data from the backend health endpoint at `http://localhost:3000/api/health`.
